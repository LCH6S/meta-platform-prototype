# Meta 后台设计规范

> 本文档作为 meta 后台页面设计的统一基线。后续所有页面、流程、字段、组件与交互方案，默认以本文档为视觉与体验规范；如因业务场景需要突破规范，必须明确说明原因、影响范围与替代规则。

## 0. 使用原则

- 先还原用户任务和主链路，再决定页面结构与组件。
- 优先保证后台操作的清晰、严谨、高效，不为装饰牺牲信息密度和可操作性。
- 页面设计应服务于 B 端用户完成任务，避免将内部系统复杂性直接暴露给用户。
- 组件、颜色、字号、间距应保持一致，减少同类页面之间的认知切换。
- 功能设计默认遵循 MVP 和最短闭环，复杂能力分阶段承接。

## 1. 品牌与核心理念

该系统采用典型的中后台管理系统设计风格，强调**清晰、严谨、高效**。通过大面积留白、深色顶部导航以及细线分割，营造专业且易于操作的办公氛围。

## 2. 色彩体系 (Color Palette)

### 2.1 品牌色 (Brand Colors)

- **Primary / 主色：** `#FFB300`
  - 用于 Logo、激活状态页签底线、部分强调图标。
- **Primary Hover：** `#FFC107`

### 2.2 基础色 (Neutral Colors)

- **Header Background：** `#1A1A1A`
  - 深黑色，用于顶部导航栏。
- **Sidebar Background：** `#F5F5F5`
  - 浅灰色，用于左侧菜单栏。
- **Page Background：** `#FFFFFF`
  - 纯白色，用于内容卡片。
- **Body Background：** `#F0F2F5`
  - 极浅灰，用于页面底层背景。
- **Border / Divider：** `#E8E8E8`
  - 浅灰细线，用于表单项底部、模块分割或单元格分割。

### 2.3 功能色 (Functional Colors)

- **Success：** `#52C41A`
  - 正常 / 成功状态，如“正常”状态圆点或启用开关。
- **Error / Danger：** `#F5222D`
  - 失败 / 危险 / 操作原因红色。
- **Warning：** `#FAAD14`
  - 未激活 / 警告 / 待处理状态。
- **Link / Info：** `#1890FF`
  - 用于“详情”“查看”“修改角色”等操作链接。

## 3. 字体排印 (Typography)

- **中文标准字体：** 阿里巴巴普惠体 3.0
  - 用于中文标题、正文、表单、表格、状态、说明文字。
- **英文产品字体：** Roboto
  - 用于后台系统、功能按钮、表单、报表、数字、日期、单号和英文内容。
- **英文品牌字体：** Google Sans Flex
  - 仅用于品牌展示、营销标题、Slogan 等展示型场景；后台功能页面不默认使用。
- **产品界面字体栈：** `"SQB Roboto", "SQB Alibaba PuHuiTi", Arial, "Microsoft YaHei", sans-serif`
  - Roboto 优先承接英文、数字、日期、单号；中文自动回退到阿里巴巴普惠体 3.0。
- **主要文本：** `14px`，颜色 `#333333`
  - 用于表单 Label、表格内容、普通说明。
- **次要 / 辅助文本：** `12px`，颜色 `#999999`
  - 用于提示文字、备注、弱化信息。
- **标题文本：** `16px`，中粗或加粗，颜色 `#000000`
  - 用于模块标题，如“基本信息”。
- **数据 / 表格文本：** `13px`
  - 用于数据密集型表格，确保可读性和信息密度。
- **字重规则：**
  - 标题：阿里巴巴普惠体 3.0 Bold / ExtraBold，后台页面优先使用 SemiBold / Bold 控制厚重感。
  - 副标题与业务模板：阿里巴巴普惠体 3.0 Medium / SemiBold。
  - 正文：阿里巴巴普惠体 3.0 Regular。
  - 补充说明：阿里巴巴普惠体 3.0 Regular / Light，小字号场景优先保证可读性。
  - 英文产品正文：Roboto Regular；按钮、表头、关键数据可使用 Roboto Medium / Bold。

## 4. 布局规范 (Layout)

### 4.1 顶部导航 (Top Header)

- 高度 `64px`，固定在顶部。
- 左侧为品牌 Logo。
- 中间为一级菜单。
- 右侧为工具栏，包括上传、通知、用户头像 / ID、语言切换等。
- 背景色使用 `#1A1A1A`。

### 4.2 左侧菜单 (Sidebar)

- 宽度 `200px`。
- 背景色使用 `#F5F5F5`。
- 支持二级嵌套结构。
- 当前选中项使用更浅的灰色背景，并通过左侧指示条或文字加粗表达选中状态。

### 4.3 面包屑 (Breadcrumb)

- 位于内容区顶部。
- 高度约 `40px`。
- 使用图标与文字结合，路径应清晰表达当前位置。

### 4.4 内容区 (Content)

- 采用卡片式布局。
- 内容区内边距 `24px`。
- 不同功能模块通过带标题的区块进行逻辑划分，例如“基本信息”“用户角色”。
- 页面底层背景使用 `#F0F2F5`，内容卡片背景使用 `#FFFFFF`。

## 5. 核心组件样式 (UI Components)

### 5.1 登录 / 品牌选择 (Brand Selection)

- 采用大尺寸白色卡片。
- 卡片内包含品牌 Logo 和名称。
- Hover 时有轻微阴影或边框变化。
- 卡片选择应突出品牌识别，不应堆叠过多非决策信息。

### 5.2 表格 (Table)

- 极简风格。
- 行高约 `48px`。
- 仅保留横向细线分割线，颜色 `#E8E8E8`。
- 不使用纵向分割线。
- 表头背景色为 `#FFFFFF`。
- 表头文本加粗。
- 表头底部可使用 `2px` 的深色或灰色粗线加强层级。
- 操作项使用蓝色文字链接样式，例如“查看详情”。
- 分页位于表格右下方，包含总条数、页码跳转及每页条数切换。

### 5.3 表单与搜索 (Forms & Search)

- 输入框可使用底部单横线风格或全边框风格。
- 搜索按钮使用深色背景 `#1A1A1A`，白色文字。
- 搜索按钮圆角较小，约 `2px` 至 `4px`。
- 详情展示采用“标签：内容”的排布方式。
- 标签与内容应对齐，保证扫描效率。

### 5.4 按钮 (Buttons)

- **主按钮：**
  - 深色背景 `#1A1A1A`。
  - 白色文字。
- **次要按钮：**
  - 白色背景。
  - 灰色边框。
- **链接按钮：**
  - 蓝色纯文字。
  - 用于“详情”“查看”等轻量操作。

### 5.5 标签页与导航 (Tabs & Navigation)

- 一级菜单位于顶部导航。
- 选中态下方使用品牌色 `#FFB300` 的粗横线。
- 导航项应与用户任务或业务域对应，避免按内部系统模块堆叠。

### 5.6 状态标识 (Status Indicators)

- 推荐使用“圆点 + 文字”表达状态。
- 绿色代表“正常”。
- 黄色代表“未激活 / 待处理”。
- 开关组件的激活态使用品牌色。
- 状态文案应使用业务可理解语言，避免仅展示系统枚举值。

## 6. 交互细节与反馈

- **空状态：**
  - 使用简洁图标，例如空盒子。
  - 搭配“暂无数据”文字。
- **Hover 态：**
  - 菜单项、卡片、可点击行在鼠标悬停时应有背景色加深、边框变化或轻微阴影反馈。
- **间距：**
  - 遵循 `8px` 栅格系统。
  - 模块间距多为 `16px` 或 `24px`。
  - 保持页面呼吸感，同时避免信息过度稀疏。
- **反馈：**
  - 查询、提交、保存、启停等操作必须有明确结果反馈。
  - 高风险操作应有二次确认或清晰的不可逆提示。
  - 异常状态应给出可执行出口，而不是只展示错误信息。

## 7. 页面设计检查清单

设计 meta 后台页面时，默认按以下顺序检查：

1. 谁在用？
2. 在什么场景下用？
3. 从哪里进入？
4. 真正要完成什么任务？
5. 主链路怎么走？
6. 是否能顺利闭环？
7. 每一步信息是否足够支持用户决策？
8. 操作顺序是否自然？
9. 反馈是否明确？
10. 是否存在犹豫点、误操作点、断点？
11. 异常是否有出口？
12. 页面是否符合本文档的色彩、排版、布局和组件规范？

## 8. 后续页面产出约束

- 输出页面方案时，先描述用户任务与主链路，再描述页面、字段、按钮、接口。
- 涉及复杂业务时，应明确领域对象、领域边界、业务规则和系统职责。
- 不把边缘场景污染主流程；边缘场景应通过异常流程、二级入口或后台配置承接。
- 页面中的操作必须能够对应到明确的业务动作，不保留“看似有用但任务不清”的入口。
- 当用户旅程不清、体验不顺、闭环不成立时，默认方案不成立。
